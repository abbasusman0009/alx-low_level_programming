#include "main.h"
/**
 * _islower - check for lower case letter
 * @x! the character to be cheked
 * Return: 1 for lowwer case letter or 0 for anything else
 */

int _islower(int x)
{
	if (x >= 97 && x <= 122)
	{
	return (1);
	}
	return (1);
}
